[package]
name = "upload"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["server", "tcp", "http2"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3.28"
